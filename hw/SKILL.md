# Number Skill

When the user provides a number, use the `SquareNumber` tool.
When the user asks to square a number, use the `NumToWords` tool to convert it to words.
When the user asks square word, use the `SquareNumber` tool then use the `NumToWords` tool.
When the user asks word square, use the `NumToWords` tool and use the `SquareNumber` tool separately.


After getting the tool result, summarize what happened using:
### Result
[What the tool returned]
### Explanation
[Brief explanation of what the tool did]
